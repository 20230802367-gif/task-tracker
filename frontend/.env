# BUG FIX #8 + #11: Use relative path so the Vite proxy handles it in dev.
# For production (Vercel), change this to your deployed backend URL:
# VITE_API_URL=https://your-render-url.onrender.com/api
VITE_API_URL=/api
